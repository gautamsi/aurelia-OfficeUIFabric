export enum PanelSize {
    small,
    medium,
    large,
    extraLarge
}
export enum PersonaShape {
    square,
    round,
}
export enum PersonaPresence {
    none,
    available,
    away,
    blocked,
    busy,
    dnd,
    offline,
}